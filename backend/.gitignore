# Dependencies
node_modules/

# Env
.env
.env.local
.env.production
.env.*.local

# Postgres CA certificate
*.pem

# Runtime data (legacy — the upload feature and its storage are both gone;
# kept as a guard so stray local dirs are never committed)
data/
data-submissions/

# Guard against a stray `cd backend` inside backend/ recreating a nested copy
backend/

# Build output
dist/
build/

# Logs
logs/
*.log
npm-debug.log*

# Test coverage
coverage/

# OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sw?
